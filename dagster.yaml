# Dagster configuration
# See: https://docs.dagster.io/deployment/dagster-instance

# Enable freshness policies (preview feature)
freshness:
  enabled: True

# Storage is controlled by the DAGSTER_HOME environment variable.
# See README.md for setup instructions.
